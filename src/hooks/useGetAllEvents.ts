/**
 * @deprecated Import from "./useGetEvents" instead; kept for stable import paths.
 * CSG President requests include `all_events=1` so the server can return every college’s events.
 */
export { useGetAllEvents, getAllEvents } from "./useGetEvents";
